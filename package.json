{
  "name": "nutrition-tracker",
  "version": "2.0.0",
  "private": true,
  "description": "A simple nutrition lookup tool powered by Claude. Auto-detects the current Claude model, so it doesn't break when Anthropic releases a new one.",
  "engines": {
    "node": ">=20"
  }
}
